# demo-repo

A fixture for the flag-cleanup example — one stale flag (`checkout_v2_enabled`, 100% rollout) whose
winning branch should be inlined and whose wrapper reference should go. Never imported by the runtime.
