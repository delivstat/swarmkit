apiVersion: swarmkit/v1
kind: Skill
metadata:
  id: route
  name: Route Request
  description: Classify a user request into an intent so the right specialist topology handles it.
category: decision
outputs:
  type: object
  properties:
    intent:
      type: string
      enum: [scenario, vision, snapshot, video, device, device_list, chat]
    reasoning:
      type: string
implementation:
  type: mcp_tool
  server: camera
  tool: route_request
provenance:
  authored_by: human
  version: 1.1.0
