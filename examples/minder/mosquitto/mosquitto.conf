# Minder MQTT broker — local single-box appliance.
# Frigate publishes tracked-object events here; the Minder webapp subscribes for
# real-time alerting. Anonymous + no persistence: it's host-only on a private box.
listener 1883
allow_anonymous true
persistence false
