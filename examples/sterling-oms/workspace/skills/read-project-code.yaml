apiVersion: swarmkit/v1
kind: Skill
metadata:
  id: read-project-code
  name: Read Project Code
  description: Read source files from the local project codebase — Java extensions, XSL transforms, XML templates, properties files.
category: capability
implementation:
  type: mcp_tool
  server: project-code
  tool: read_file
iam:
  required_scopes: [repo:read]
provenance:
  authored_by: human
  version: 1.0.0
