{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.swarmkit.dev/v1/archetype.schema.json",
  "title": "SwarmKit Archetype",
  "description": "A kind of agent (noun). See design §6.6, §13 and design/details/archetype-schema-v1.md.",
  "type": "object",
  "required": ["apiVersion", "kind", "metadata", "role", "defaults", "provenance"],
  "additionalProperties": false,
  "properties": {
    "apiVersion": { "const": "swarmkit/v1" },
    "kind": { "const": "Archetype" },
    "metadata": { "$ref": "#/$defs/metadata" },
    "role": {
      "enum": ["root", "leader", "worker"],
      "description": "Must match the agent role where this archetype is instantiated."
    },
    "defaults": { "$ref": "#/$defs/defaults" },
    "provenance": { "$ref": "#/$defs/provenance" }
  },
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "semver": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "description"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "name": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 10 }
      }
    },
    "defaults": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "model": { "$ref": "#/$defs/model" },
        "prompt": { "$ref": "#/$defs/prompt" },
        "skills": {
          "type": "array",
          "items": { "$ref": "#/$defs/skill_entry" }
        },
        "iam": { "$ref": "#/$defs/iam" },
        "output_schema": {
          "oneOf": [
            {
              "type": "object",
              "description": "JSON Schema for structured inter-agent output. When set, the agent produces JSON instead of prose, and the compiler requests JSON mode from the model provider.",
              "additionalProperties": true
            },
            {
              "type": "null",
              "description": "Explicitly opt out of the platform default output schema."
            }
          ]
        }
      }
    },
    "model": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "provider": { "type": "string" },
        "name": { "type": "string" },
        "temperature": { "type": "number", "minimum": 0, "maximum": 2 },
        "max_tokens": { "type": "integer", "minimum": 1 },
        "tool_provider": {
          "type": "string",
          "description": "Model provider for tool-calling turns (cheaper model). Falls back to provider if unset."
        },
        "tool_model": {
          "type": "string",
          "description": "Model name for tool-calling turns. When set, tool loop uses this cheaper model and synthesis uses the main model."
        }
      }
    },
    "prompt": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "system": { "type": "string" },
        "persona": { "type": "string" }
      }
    },
    "skill_entry": {
      "$comment": "Concrete skill ID (string) or abstract-skill placeholder (§6.6 edge case).",
      "oneOf": [
        { "$ref": "#/$defs/identifier" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["abstract"],
          "properties": {
            "abstract": {
              "type": "object",
              "additionalProperties": false,
              "required": ["category"],
              "properties": {
                "category": {
                  "enum": ["capability", "decision", "coordination", "persistence"]
                },
                "capability": {
                  "type": "string",
                  "description": "Free-text tag matched by the topology resolver."
                }
              }
            }
          }
        }
      ]
    },
    "iam": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "base_scope": { "type": "array", "items": { "type": "string" } },
        "elevated_scopes": { "type": "array", "items": { "type": "string" } }
      }
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["authored_by", "version"],
      "properties": {
        "authored_by": {
          "enum": [
            "human",
            "authored_by_swarm",
            "derived_from_template",
            "imported_from_registry",
            "vendor_published"
          ]
        },
        "authored_date": { "type": "string", "format": "date" },
        "version": { "$ref": "#/$defs/semver" },
        "registry": { "type": "string" },
        "vendor": { "type": "string" }
      }
    }
  }
}
