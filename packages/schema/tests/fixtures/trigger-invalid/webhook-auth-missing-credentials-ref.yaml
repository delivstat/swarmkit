# webhook auth requires a credentials_ref — never inline secrets.
apiVersion: swarmkit/v1
kind: Trigger
metadata:
  id: broken
  name: Broken
type: webhook
targets:
  - hello-swarm
config:
  path: /webhook
  auth:
    method: hmac
