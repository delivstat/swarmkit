# Invalid — quorum must be "all", "any", or {k-of: N}.
apiVersion: swarmkit/v1
kind: Funnel
metadata:
  id: bad-quorum
  name: Bad Quorum
  description: The approve rule uses an unsupported quorum mode.
approve:
  rules:
    - scope: design:approve
      roles: [oms-lead]
      quorum: majority
provenance:
  authored_by: human
  version: 1.0.0
