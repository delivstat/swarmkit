# Smallest valid trigger — fired by `swael trigger fire <id>` or the
# "Run now" UI button. No config required.
apiVersion: swael/v1
kind: Trigger
metadata:
  id: run-now
  name: Run Now
targets:
  - hello-swarm
type: manual
