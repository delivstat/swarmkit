# The smallest valid topology: a single root agent with an archetype reference.
apiVersion: swael/v1
kind: Topology
metadata:
  name: minimal
  version: 0.1.0
agents:
  root:
    id: root
    role: root
    archetype: supervisor-root
