# Smallest valid workspace — metadata only; runtime applies defaults for
# governance (agt), identity (builtin), storage (sqlite).
apiVersion: swael/v1
kind: Workspace
metadata:
  id: hello-workspace
  name: Hello Workspace
