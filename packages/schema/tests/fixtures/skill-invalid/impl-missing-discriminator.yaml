# implementation requires a `type` discriminator.
apiVersion: swael/v1
kind: Skill
metadata:
  id: broken
  name: Broken
  description: Implementation block lacks the `type` discriminator.
category: capability
implementation:
  server: foo
  tool: bar
provenance:
  authored_by: human
  version: 1.0.0
