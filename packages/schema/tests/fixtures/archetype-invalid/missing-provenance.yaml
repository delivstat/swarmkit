# provenance is required on every archetype (design §13).
apiVersion: swael/v1
kind: Archetype
metadata:
  id: broken
  name: Broken
  description: Archetype without the required provenance block.
role: worker
defaults: {}
