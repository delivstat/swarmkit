# source=plugin requires provider_id so the runtime knows which registered
# SecretsProvider to dispatch to.
apiVersion: swael/v1
kind: Workspace
metadata:
  id: broken
  name: Broken
credentials:
  acme_internal:
    source: plugin
    config:
      service: swael
      key: internal-token
