# source must be one of the recognised enum values.
apiVersion: swael/v1
kind: Workspace
metadata:
  id: broken
  name: Broken
credentials:
  anthropic_api_key:
    source: bitwarden          # not yet a recognised SecretsProvider; use plugin instead
    config:
      item: swael-anthropic
      field: api_key
