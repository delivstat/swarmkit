apiVersion: swarmkit/v1
kind: Skill
metadata:
  id: git-diff
  name: Git Diff
  description: The diff between two revisions, or the working tree against HEAD.
category: capability
implementation: {type: mcp_tool, server: git, tool: git_diff}
iam: {required_scopes: [workspace:read]}
provenance: {authored_by: human, version: 1.0.0, registry: swarmkit-skills}
