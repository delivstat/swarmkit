apiVersion: swael/v1
kind: Skill
metadata:
  id: broken-panel
  name: Broken Panel
  description: References a nonexistent skill; registry must reject.
category: decision
outputs:
  type: object
  properties:
    verdict: { type: string }
    reasoning: { type: string }
  required: [verdict, reasoning]
implementation:
  type: composed
  composes:
    - not-defined-anywhere
provenance:
  authored_by: human
  version: 0.1.0
