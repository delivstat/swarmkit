# This workspace has topologies/ but no workspace.yaml at the root.
# Discovery must fail with MissingWorkspaceFileError.
apiVersion: swarmkit/v1
kind: Topology
metadata:
  name: hello
  version: 0.1.0
agents:
  root:
    id: root
    role: root
