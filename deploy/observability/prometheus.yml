global:
  scrape_interval: 15s

scrape_configs:
  # Scrape the metrics the OTel collector re-exports (swarmkit.* counters/histograms).
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
