---
description: Ask a design question — looks it up in the v0.6 doc and answers with section citations.
argument-hint: <question about the design>
---

Use the `design-lookup` agent to answer: $ARGUMENTS

If the question touches an open item in §21, flag it explicitly.
