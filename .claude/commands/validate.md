---
description: Validate a SwarmKit YAML artifact against the canonical JSON Schemas.
argument-hint: <path to yaml file>
---

Use the `schema-validator` agent to validate the artifact at: $ARGUMENTS
